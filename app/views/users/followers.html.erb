<div class="row">
    <div class="col-md-3">
      <h3>フォロワー</h3>
        <% @user.followers.each do |user| %>
          <%= link_to user.name, user_path(user [:id]) %>
        <% end %>
    </div>
</div>