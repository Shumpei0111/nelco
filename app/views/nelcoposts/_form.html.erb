■render _form.html.erb ここから■

<%= form_for(@nelcopost) do |f| %>
  <% if @nelcopost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nelcopost.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @nelcopost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
  <%= f.label :タイトルを入力してください（20文字まで） %><br>
  <%= f.text_field :title %>
</div>

<div class="field">
  <%= f.label :画像 %>
  <%= f.file_field :image %>
</div>
<br>
<div class="actions">
  <%= f.submit "画像を追加する" %>
</div>

<% end %>

■render _form.html.erb ここまで■